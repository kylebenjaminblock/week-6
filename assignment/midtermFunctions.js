/* ================================
Week 6 Assignment: Midterm Functions + Signatures
================================ */

// Display the wealthiest and poorest country in the dataset
 var highLow = filter(data, function(each){
   /// filter to select the country with highest and lowest income and display to map
 }

// Produce Objects to Add Points and Markers
 var getIncome = function (country, income){
   /// call and return income data by country
 };

 // Returns array of objects (countries) with the life expectancy for each
 var  getLifeExpectancy = function(country, lifeExpectancy){
   /// call and return data from JSON dataset and add to map
 };
